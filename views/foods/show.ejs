<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= food.name %></title>
</head>
<body>
    <h1><%= food.name %></h1>
    <h2>Food storing directions</h2>
    <p>This food should best be kept <%= food.mustKeepRefregerated ? 'in refregerater.' : 'out of refregerater.' %></p>
    <p>And this food should best be kept <%= food.mustkeepFrozen ? 'in freezer.' : 'out of freezer.' %></p>
    <br/>
    <form action="/foods/<%= food._id %>?_method=DELETE" method="POST">
        <button type="submit">Delete <%= food.name %></button>
    </form>
    <br>
    <a href="/foods/<%= food._id %>/edit">Edit <%= food.name %></a>
    <a href="/foods/">Back to foods</a>
</body>
</html>