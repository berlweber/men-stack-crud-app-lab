<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Update <%= food.name %></h1>
    <form action="/foods/<%= food.id %>?_method=PUT" method="POST">
        <label for="name">Name:</label>
        <input type="text" name="name" id="name" value="<%= food.name %>"/>

        <label for="mustKeepRefregerated">Must keep it refregerated?</label>
        <input type="checkbox" name="mustKeepRefregerated" id="mustKeepRefregerated" 
        <%  if (food.mustKeepRefregerated) { %> checked <% } %> >

        <label for="mustkeepFrozen">Must keep it frozen?</label>
        <input type="checkbox" name="mustkeepFrozen" id="mustkeepFrozen"  
        <%  if (food.mustkeepFrozen) { %> checked <% } %> >

        <button type="submit">Update <%= food.name %></button>
    </form>
    <br>
     <a href="/foods/">Back to Foods</a>
</body>
</html>